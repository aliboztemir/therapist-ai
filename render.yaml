services:
  - type: web
    name: therapist-ai
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /actuator/health

    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: TURNSTILE_SECRET_KEY
        sync: false   # Get this from Cloudflare Turnstile dashboard
